$ cd <fresh temp dir> && groundwork init --adapter claude-code
$ CMD = hooks.SessionStart[0].hooks[0].command from .claude/settings.json
hook command: node "$CLAUDE_PROJECT_DIR/.groundwork/hooks/session-start.mjs"
$ CLAUDE_PROJECT_DIR=<temp dir> sh -c "$CMD"      (Git Bash on Windows 11, Node 22)
--- fresh install:
{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"Groundwork: no card in progress.\nNext: run `gw-setup`\nType /gw to continue."}}
--- card in progress:
{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"Groundwork: card 1.3 Login flow is being built.\nNext: make the session test pass\nType /gw to continue."}}

Run before the HANDOFF template's next step was reworded to "set up the project: type `/gw` (it runs `gw-setup`)".
Not a live Claude Code session: that check is on card 6.1.
