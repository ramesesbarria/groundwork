OpenCode v2.0.3, installed on this machine. Project freshly set up with: groundwork init --adapter opencode

$ opencode debug config   (config sources)
    "type": "directory",
    "path": "C:\\Users\\rames\\.config\\opencode"
--
    "type": "directory",
    "path": "C:\\Users\\rames\\AppData\\Local\\Temp\\tmp.ReOjGn9ajK\\.opencode"

$ opencode debug agents   (Groundwork agents, their mode and denied permissions)
gw-implementer mode=subagent deny=["shell:sudo *","shell:shutdown *","shell:reboot *","shell:git push --force*","shell:git reset --hard *"]
gw-planner mode=subagent deny=["shell:sudo *","shell:shutdown *","shell:reboot *","shell:git push --force*","shell:git reset --hard *","shell:*"]
gw-reviewer mode=subagent deny=["shell:sudo *","shell:shutdown *","shell:reboot *","shell:git push --force*","shell:git reset --hard *","edit:*"]
gw-tester mode=subagent deny=["shell:sudo *","shell:shutdown *","shell:reboot *","shell:git push --force*","shell:git reset --hard *"]

Guard plugin: not confirmed inside OpenCode. No log line shows plugins loading without a model session; that check needs a real session (card 6.1).
