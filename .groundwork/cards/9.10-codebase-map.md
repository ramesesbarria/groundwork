---
id: 9.10
title: Codebase map
phase: 9
status: done
depends_on: [7.11]
---
## Goal
Setup leaves a short map of where things live, so every role loads less of an unfamiliar codebase. Planned changes: PLAN.md §9.10.

## Acceptance criteria
- [x] The map exists after existing-project setup and stays under the size limit
- [x] Tester, implementer and reviewer name it in their Load lists

## How to check
Read step 3 of `core/guides/existing-project.md` (the map), the "Codebase map" section in `core/templates/SPEC.md`, and the Load list in any of the three build roles.

## Evidence
- Tests before: 8 fail ([output](../evidence/9.10/failing-before.txt)); after: 488 pass, and the type check passes ([output](../evidence/9.10/test-output.txt))
- Existing-project setup step 3 adds a Codebase map to SPEC.md: main folders, entry points, where tests live, anything unusual, each marked *found* or *guessed*, under about 20 lines. The SPEC template has an empty section for it; `gw-plan` adds new areas as cards create them.
- The tester, implementer and reviewer Load lists read the map first, then only the files it points to.
- `roles.test.ts` checked that the implementer never loads SPEC.md. It now checks that any SPEC.md mention in the implementer's Load list is the Codebase map, which keeps its purpose: never the whole spec.
- Reviewer: all criteria met. "Exists after setup" is an instruction to the agent, so it's checked in the guide's text, not by running setup. Checked against L-006, L-017 and L-018.

## History
- 2026-09-24 created by planner from PLAN.md
- 2026-09-25 testing: 8 failing tests in `cli/test/codebase-map.test.ts`
- 2026-09-25 implementing: map step in the guide, SPEC section, gw-plan and role Load lists
- 2026-09-25 review → done (the human asked for all planned changes without per-card stops)
