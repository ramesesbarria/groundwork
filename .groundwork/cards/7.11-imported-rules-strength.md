---
id: 7.11
title: Imported rules keep their strength
phase: 7
status: done
depends_on: [7.10]
---
## Goal
The rules a project already follows don't get demoted to NOTEs that only the reviewer reads. Planned changes: PLAN.md §7.11.

## Acceptance criteria
- [x] The step says imported rules can be kept as RULEs
- [x] Nothing is dropped without being shown to the human first
- [x] `existing-projects.test.ts` updated

## How to check
Read step 5 in `core/guides/existing-project.md`: each rule is imported, you're asked which stay always-on, those go into AGENTS.md's Rules section, and you see the split before anything is written.

## Evidence
- Tests before: 5 fail ([output](../evidence/7.11/failing-before.txt)); after: 374 pass, and the type check passes ([output](../evidence/7.11/test-output.txt))
- Structural change: the new step pushed `gw-setup` over its 800-token cap, and cards 9.8–9.11 and 10.1 add more to the same path. The "Existing project" steps moved to `core/guides/existing-project.md`, which `gw-setup`'s Purpose links to; `gw-setup` is now ≈517 tokens. `init` already copies every core folder, so no CLI change was needed.
- Tests that read the old section now read the guide. One check in `plain-setup.test.ts` would have passed on an empty section after the move, so it now asserts the guide's wording directly. A new test checks `gw-setup` links to the guide.
- PLAN.md 10.5 now points to the guide.
- Reviewer: all criteria met. Checked against L-006, L-014, L-017 (grepped for the old section name) and L-018.
- Caveat on L-014: removing the old section from `gw-setup.md` was done with a short Node script, not the edit tool. It only touched markdown and the result was checked.

## History
- 2026-09-24 created by planner from PLAN.md
- 2026-09-24 testing: 5 failing tests in `cli/test/existing-projects.test.ts`
- 2026-09-24 implementing: import step keeps always-on rules as RULEs; existing-project steps moved to a guide to stay under the command budget
- 2026-09-24 review → done (the human asked for all planned changes without per-card stops)
