---
id: 5.1
title: groundwork doctor
phase: 5
status: done
depends_on: [4.6]
---
## Goal
Harness health and the context budget (SPEC §10).

## Acceptance criteria
- [x] Reports the always-loaded token estimate against `tokenBudget`, and exits non-zero when over
- [x] Flags guards listed in config that don't exist, and adapter files that differ from what the core would generate
- [x] Flags lessons never cited in any card (candidates to archive) and rules in AGENTS.md without a lesson ID
- [x] Flags cards whose status disagrees with their History, e.g. History says approved but status isn't `done` (L-016)
- [x] Plain-English output: one line per problem, with the fix

## Evidence
- Tests before: 9 fail ([output](../evidence/5.1/failing-before.txt)); after: 251 pass ([output](../evidence/5.1/test-output.txt)); build OK ([output](../evidence/5.1/build-output.txt))
- **Real use:** run read-only on the v0.1 DogFood project. It correctly reports that its `.claude/settings.json` has no guard hook (v0.1 had no guards) and exits 1 ([output](../evidence/5.1/doctor-on-dogfood.txt))
- Reviewer: all criteria met. Checked against L-003, L-006, L-016 (this card adds the status-vs-History check).
  - Problems exit 1, so `doctor` can run in CI; suggestions (uncited lessons, rules without an ID) don't fail it.
  - A user's own CLAUDE.md only has to contain `@AGENTS.md`; it isn't flagged for differing.
  - **Gap:** `doctor` compares adapter files with the project's own `.groundwork/`, so it can't see that a project is on an older Groundwork than the CLI (DogFood lacks `gw-quick` and others). An `upgrade` command is future work.
  - The token count uses the characters ÷ 4 estimate (SPEC open question 1).
  - One wording change so the output matched the test ("never cited").

## History
- 2026-09-24 tests → implementation → real run on DogFood → review; committed
- 2026-09-24 created by planner after the dogfood run
