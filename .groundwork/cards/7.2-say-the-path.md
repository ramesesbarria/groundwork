---
id: 7.2
title: Say the path out loud
phase: 7
status: done
depends_on: [7.1]
---
## Goal
When a request arrives without a command, the agent says in one line which path it's taking (quick or card) and lets the human override it. Planned changes: PLAN.md §7.2.

## Acceptance criteria
- [x] The rule names both paths and the override
- [x] The AGENTS.md template is still about 400 tokens (the templates test passes, and `doctor` on a fresh install shows no budget problem)

## Evidence
- Tests before: 3 of 6 new tests fail; the 3 that already pass guard the gw-quick mention, the 9-item list and the token cap ([output](../evidence/7.2/failing-before.txt)). After: 324 pass, and the type check passes ([output](../evidence/7.2/test-output.txt))
- Rule 9 in `core/templates/AGENTS.md` was rewritten in place, not added, so "How we work" still has 9 items.
- Size: the template went from ≈411 to ≈438 tokens. The new test caps it at 450.
- `doctor` on a fresh install in a temp folder: "No problems found", ≈514 tokens always loaded out of a 2,000 budget; that figure includes the generated CLAUDE.md ([output](../evidence/7.2/doctor-fresh-install.txt))
- Reviewer: all criteria met. Checked against L-006, L-015 (`init` ran only after changing into the temp folder; `git status` confirmed this repo was untouched), L-017 (no other file quoted the old rule 9) and L-018.
- Caveats:
  - The rule is text. Whether an agent actually says the path before starting is only visible in a live session (card 6.1).

## History
- 2026-09-24 created by planner from PLAN.md
- 2026-09-24 testing: 3 failing tests in `cli/test/say-the-path.test.ts`
- 2026-09-24 implementing: rule 9 rewritten to name both paths and the override
- 2026-09-24 review → awaiting-approval
- 2026-09-24 approved by human
