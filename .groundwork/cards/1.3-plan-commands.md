---
id: 1.3
title: Setup, spec and plan commands
phase: 1
status: done
depends_on: [1.1]
---
## Goal
Tool-neutral command files for `gw-setup` (new projects only in v0.1), `gw-spec` and `gw-plan`.

## Acceptance criteria
- [x] `core/commands/` has one markdown file per command: purpose, steps, files written
- [x] `gw-spec` asks specific questions in batches, never "do you agree with section N?"
- [x] `gw-plan` writes one card file per card, with acceptance criteria and `depends_on`
- [x] `gw-setup` writes AGENTS.md and config.json from a short interview, and picks no stack for the user

## Evidence
- Tests before: 21 new tests fail with no command files ([output](../evidence/1.3/failing-before.txt))
- Tests after: 66 passed, 21 of them new ([output](../evidence/1.3/test-output.txt))
- Build: `tsc` succeeds ([output](../evidence/1.3/build-output.txt))
- Size: each command file is between about 348 and 481 tokens, under 800 ([estimate](../evidence/1.3/token-estimate.txt))
- Reviewer (second pass): all criteria met. Checked against L-003, L-005 and L-006. The "specific questions, never section-level ones" rule from the /insights report is now written into gw-spec.
  - Added beyond the card: `name`/`description` frontmatter on each command, which the Claude Code adapter (2.1) will need.
  - `gw-setup` refers to `npx groundwork-ai init`, which doesn't exist until card 2.2.
  - First pass sent back: planner.md didn't allow what gw-plan and gw-spec do. Fixed in `core/roles/planner.md`.

## History
- 2026-09-24 tester → implementer → reviewer (sent back once, then passed); committed in per-phase mode, awaiting phase 1 approval
- 2026-09-24 review → implementing: gw-plan writes commands to config.json and AGENTS.md, and gw-spec reads notes the human points to, but roles/planner.md allows neither
