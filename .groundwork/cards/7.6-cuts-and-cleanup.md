---
id: 7.6
title: Cuts and cleanup
phase: 7
status: done
depends_on: [5.3]
---
## Goal
No empty placeholders or dead code, and SPEC and README agree that evals are later, not a current goal. Planned changes: PLAN.md §7.6.

## Acceptance criteria
- [x] No empty placeholder directories
- [x] No dead code path in `index.ts`
- [x] SPEC §12 matches the real repo layout
- [x] SPEC and README agree on the current goal

## How to check
Run `ls` in the repo root: there's no `adapters/`, `docs/`, `examples/` or `guards/`. Then read SPEC §13: v1.0 is "Proven in use", and evals are "later, not a current goal".

## Evidence
- Tests before: 5 fail ([output](../evidence/7.6/failing-before.txt)); after: 345 pass, and the type check passes ([output](../evidence/7.6/test-output.txt))
- Removed the three tracked `.gitkeep` placeholders, plus an untracked empty `guards/` folder at the root.
- `index.ts`: every name in `COMMANDS` has its own branch above, so the "planned but not implemented" branch could never run. Unknown commands still get the usage message.
- SPEC: §12 rewritten from the real tree, §13 gets a v0.5 row and a v1.0 without evals, §14 drops the eval table, and the §1 comparison no longer promises one. §12 no longer lists a `no-secrets` guard, which doesn't exist.
- Reviewer: all criteria met. Checked against L-006 (the SPEC listed a guard that doesn't exist; fixed), L-017 (grepped SPEC and README for "benchmark" and "eval") and L-018.

## History
- 2026-09-24 created by planner from PLAN.md
- 2026-09-24 testing: 5 failing tests in `cli/test/cleanup.test.ts`
- 2026-09-24 implementing: placeholders removed, dead branch removed, SPEC and README updated
- 2026-09-24 review → done (the human asked for all planned changes without per-card stops)
