---
id: 4.5
title: gw-decide and gw-ui-spec
phase: 4
status: todo
depends_on: [3.2]
---
## Goal
The two remaining commands from SPEC §7.

## Acceptance criteria
- [ ] `gw-decide <topic>` writes a decision record with 2–4 options, waits for the human's choice, records it in their words, and updates the commands if the stack changed
- [ ] `gw-ui-spec` writes states, transitions, constraints and screen sizes as checkable criteria on the card before UI work, and waits for the human's OK (L-004)
- [ ] `gw-plan` points to `gw-decide` for decisions instead of repeating its steps

## Evidence

## History
- 2026-09-24 created by planner after the dogfood run
