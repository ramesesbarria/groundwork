---
id: 4.5
title: gw-decide and gw-ui-spec
phase: 4
status: done
depends_on: [3.2]
---
## Goal
The two remaining commands from SPEC §7.

## Acceptance criteria
- [x] `gw-decide <topic>` writes a decision record with 2–4 options, waits for the human's choice, records it in their words, and updates the commands if the stack changed
- [x] `gw-ui-spec` writes states, transitions, constraints and screen sizes as checkable criteria on the card before UI work, and waits for the human's OK (L-004)
- [x] `gw-plan` points to `gw-decide` for decisions instead of repeating its steps

## Evidence
- Tests before: 20 fail ([output](../evidence/4.5/failing-before.txt)); after: 232 pass ([output](../evidence/4.5/test-output.txt)); build OK ([output](../evidence/4.5/build-output.txt))
- Reviewer: all criteria met. Checked against L-004 (this is its fix: spec the behavior before UI code), L-006 and L-011 ("your call" isn't accepted for decisions).
  - `gw-ui-spec` bakes in the specific failures from the testimonials carousel: say which elements move (real element vs. overlay), no scrollbars, no clipping fixed heights, and commit a known-good state before a redesign.
  - Settles SPEC open question 3: UI criteria are verified by screenshots when the tool can take them, otherwise by manual checks.
  - The snapshot changes were reviewed: only the two new commands.

## History
- 2026-09-24 tests → implementation → review; committed
- 2026-09-24 created by planner after the dogfood run
