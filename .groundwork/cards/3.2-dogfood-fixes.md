---
id: 3.2
title: Dogfood fixes
phase: 3
status: done
depends_on: [3.1]
---
## Goal
Fix what the dogfood run found (card 3.1 F1–F8; lessons L-003, L-007 to L-012).

## Acceptance criteria
- [x] `gw-spec` asks for the smallest useful first version before details, and moves the rest to later phases (L-003, now a rule; also in `roles/planner.md`)
- [x] `gw-spec` says what to do when the human says "you decide": use the suggested answers, mark them *(default)*, list any extra choices (L-011)
- [x] `gw-plan` checks the spec was confirmed, and updates HANDOFF before stopping to wait for decisions (L-007, L-008)
- [x] The tester adds manual checks only for criteria that can't be automated (L-009)
- [x] The AGENTS.md template's commit rule is correct in both approval modes (L-010)
- [x] `init` writes a `.gitattributes` for Groundwork's files, and `gw-setup` ends by suggesting a commit (L-012)

## Evidence
- Tests before: 11 new tests fail ([output](../evidence/3.2/failing-before.txt))
- Tests after: 154 passed ([output](../evidence/3.2/test-output.txt)); build succeeds ([output](../evidence/3.2/build-output.txt))
- Reviewer: all criteria met. Checked against L-003, L-006, L-007 to L-012.
  - `init` *adds* its lines to an existing `.gitattributes` without asking, since adding lines can't lose anything, and never duplicates them.
  - The first `init.ts` edit broke the file (escapes turned into line breaks); restored from git and redone. Logged as L-014.

## History
- 2026-09-24 tests → fixes → review; committed
- 2026-09-24 created by planner after the dogfood run
