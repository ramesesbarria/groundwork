---
id: 3.2
title: Dogfood fixes
phase: 3
status: todo
depends_on: [3.1]
---
## Goal
Fix what the dogfood run found (card 3.1 F1–F8; lessons L-003, L-007 to L-012).

## Acceptance criteria
- [ ] `gw-spec` asks for the smallest useful first version before details, and moves the rest to later phases (L-003, now a rule; also in `roles/planner.md`)
- [ ] `gw-spec` says what to do when the human says "you decide": use the suggested answers, mark them *(default)*, list any extra choices (L-011)
- [ ] `gw-plan` checks the spec was confirmed, and updates HANDOFF before stopping to wait for decisions (L-007, L-008)
- [ ] The tester adds manual checks only for criteria that can't be automated (L-009)
- [ ] The AGENTS.md template's commit rule is correct in both approval modes (L-010)
- [ ] `init` writes a `.gitattributes` for Groundwork's files, and `gw-setup` ends by suggesting a commit (L-012)

## Evidence

## History
- 2026-09-24 created by planner after the dogfood run
