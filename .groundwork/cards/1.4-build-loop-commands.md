---
id: 1.4
title: Build loop commands
phase: 1
status: done
depends_on: [1.2]
---
## Goal
`gw-next`, `gw-approve` and `gw-reject`: the heart of the loop.

## Acceptance criteria
- [x] `gw-next` picks the lowest-numbered card whose dependencies are all `done`, and runs tester → implementer → reviewer
- [x] `gw-next` stops at `awaiting-approval` in per-card mode
- [x] `gw-approve` refuses when Evidence is empty or a linked file is missing, and otherwise commits as `[<id>] <title>`
- [x] `gw-reject <reason>` records the reason under History and sends the card back to the implementer

## Evidence
- Tests before: 23 new tests fail with no command files ([output](../evidence/1.4/failing-before.txt)). The first try had a broken regex that stopped the test file loading at all; it was fixed and rerun before this output was saved.
- Tests after: 90 passed, 24 of them new ([output](../evidence/1.4/test-output.txt))
- Build: `tsc` succeeds ([output](../evidence/1.4/build-output.txt))
- Size: 286 to 555 tokens per command, under 800 ([estimate](../evidence/1.4/token-estimate.txt))
- Reviewer (second pass): all criteria met. Checked against L-001 (gw-approve checks LESSONS and AGENTS for commit-message rules), L-003 and L-006.
  - **The evidence check is done by the AI following instructions, not enforced by code.** It's a good candidate for a CLI check or guard in v0.2.
  - gw-approve also refuses when a criterion is unchecked with no explanation. That's stricter than workflow.md says; they don't conflict.
  - gw-next refers to `gw-resume`, which arrives in card 1.5.
  - First pass sent back: there was no way to approve a phase in per-phase mode. Added to gw-approve, with a test.

## History
- 2026-09-24 tester → implementer → reviewer (sent back once, then passed); committed in per-phase mode, awaiting phase 1 approval
- 2026-09-24 review → implementing: per-phase mode has no way to approve a phase; gw-approve only handles single cards
