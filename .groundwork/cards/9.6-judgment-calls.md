---
id: 9.6
title: Judgment calls, logged and shown
phase: 9
status: done
depends_on: [7.8]
---
## Goal
In `per-phase` mode the agent keeps working instead of stalling on small questions, but every judgment call is written down and shown at approval. Planned changes: PLAN.md §9.6.

## Acceptance criteria
- [x] The stop list is written in plain words
- [x] Phase approval shows every call from that phase's cards
- [x] A retro test finds a call that was followed by a rejection

## How to check
Read "Stop only for" in `core/workflow.md`: four reasons to stop, and a History line format for everything else. Then look at step 7 of `gw-next`: the approval summary lists the card's calls.

## Evidence
- Tests before: 6 fail ([output](../evidence/9.6/failing-before.txt)); after: 456 pass, and the type check passes ([output](../evidence/9.6/test-output.txt))
- Stop only for: anything destructive or that can't be undone, security-sensitive changes, a new dependency or stack change (needs a decision record), and a spec gap that changes what users see. Everything else becomes `YYYY-MM-DD call: <what> — <why> — <cost if wrong>` on the card.
- Shown at approval: `gw-next` step 7 now has a fourth part, "Calls", and phase approval in `gw-approve` includes every `call:` line from the phase's cards. The card template shows the format next to the others from card 7.8.
- `groundwork retro` has a new "Calls later rejected" section. A call counts when a rejection follows it on the same card. It's left out of the "By lesson cited" counts, because it repeats the rejection's text (a test covers this).
- Reviewer: all criteria met. Checked against L-006, L-017 and L-018. Wording is original; the idea is borrowed from superpowers' "rulings, not stalls", as PLAN.md notes.

## History
- 2026-09-24 created by planner from PLAN.md
- 2026-09-24 testing: 6 failing tests in `cli/test/judgment-calls.test.ts`
- 2026-09-24 implementing: stop list, call format, calls in approval summaries, retro section
- 2026-09-24 review → done (the human asked for all planned changes without per-card stops)
