---
id: 7.5
title: "README: Your first 10 minutes"
phase: 7
status: done
depends_on: [7.3, 7.4]
---
## Goal
A newcomer sees what using Groundwork feels like before reading about roles and ledgers. Planned changes: PLAN.md §7.5.

## Acceptance criteria
- [x] The transcript is under about 30 lines and uses only the commands a first-timer needs
- [x] The approval stop in the transcript uses the 7.3 format
- [x] `cli/README.md` has a two-line version
- [x] `readme.test.ts` passes without being weakened

## How to check
Open the README on GitHub (or in a markdown preview) and read from the top: the example session comes right after Install.

## Evidence
- Tests before: 6 fail ([output](../evidence/7.5/failing-before.txt)); after: 340 pass, and the type check passes ([output](../evidence/7.5/test-output.txt))
- The transcript is 22 lines and uses `/gw-spec`, `/gw-plan`, `/gw-next` and `/gw-approve`. `readme.test.ts` is unchanged; the mermaid diagram and install command are still before `## Why`.
- Reviewer: all criteria met. Checked against L-006 (the transcript is labeled as an example, not a recorded session), L-017 and L-018.
- Caveats: "How it works" wasn't moved lower; the page reads fine with the example above it. Card 8.1 will update the example when `/gw` exists.

## History
- 2026-09-24 created by planner from PLAN.md
- 2026-09-24 testing: 6 failing tests in `cli/test/first-ten-minutes.test.ts`
- 2026-09-24 implementing: example session in both READMEs
- 2026-09-24 review → done (the human asked for all planned changes without per-card stops)
