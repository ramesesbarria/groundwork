---
id: 1.2
title: Workflow document
phase: 1
status: done
depends_on: [1.1]
---
## Goal
`core/workflow.md`: the whole loop step by step, readable by any AI tool with no adapter.

## Acceptance criteria
- [x] Covers card statuses and allowed transitions (SPEC §8)
- [x] Covers both approval modes, the evidence rule and the commit format `[<id>] <title>`
- [x] Says when HANDOFF.md is updated: at every role boundary and before stopping
- [x] Explains how to run the roles one after another in a tool without subagents

## Evidence
- Tests before: the new test file fails because workflow.md doesn't exist ([output](../evidence/1.2/failing-before.txt))
- Tests after: 45 passed, 9 of them new ([output](../evidence/1.2/test-output.txt))
- Build: `tsc` succeeds ([output](../evidence/1.2/build-output.txt))
- Size: workflow.md ≈ 976 tokens, under 1,500 ([estimate](../evidence/1.2/token-estimate.txt))
- Reviewer (second pass): all criteria met. Checked against L-003 and L-006.
  - The status-change table in workflow.md is tested against `TRANSITIONS` in `cli/src/schema.ts`, so the docs and code can't drift apart.
  - It settles the two gaps from 1.1: the **runner** starts cards and does all commits after approval.
  - First pass sent back: reviewer.md contradicted per-phase mode. Fixed in `core/roles/reviewer.md`.

## History
- 2026-09-24 tester → implementer → reviewer (sent back once, then passed); committed in per-phase mode, awaiting phase 1 approval
- 2026-09-24 review → implementing: `roles/reviewer.md` says a pass always goes to `awaiting-approval`, which contradicts `per-phase` mode (pass → `done`)
