---
id: 7.8
title: History lines that tools can parse
phase: 7
status: done
depends_on: [5.3]
---
## Goal
Agents write card History in the formats `groundwork retro` and `doctor` parse. Planned changes: PLAN.md §7.8.

## Acceptance criteria
- [x] The template formats match the regexes in `retro.ts` and `doctor.ts`
- [x] A test builds a card from the template examples and `retro` finds all the signals

## How to check
Open `core/templates/card.md`: the History comment lists the three formats. Run `npx groundwork-ai retro` in a project with a rejected card, and the rejection shows up.

## Evidence
- Tests before: 4 fail ([output](../evidence/7.8/failing-before.txt)); after: 354 pass, and the type check passes ([output](../evidence/7.8/test-output.txt))
- Bug found while testing and fixed in this card: `retro` and `doctor` read HTML comments inside History. With the new examples in the template, every fresh card would have shown a fake rejection, a fake send-back and a fake approval. Both now strip comments first, using the helper `doctor` already had (moved to `frontmatter.ts`). A test covers a card with the template comment left in.
- `doctor` on this repo: no problems.
- Reviewer: all criteria met. Checked against L-006, L-016 (a status check that silently misfires is exactly what the comment bug would have caused), L-017 and L-018.

## History
- 2026-09-24 created by planner from PLAN.md
- 2026-09-24 testing: 4 failing tests in `cli/test/history-format.test.ts`, plus 1 for template comments
- 2026-09-24 implementing: formats in the template, workflow points to them, History comments ignored by retro and doctor
- 2026-09-24 review → done (the human asked for all planned changes without per-card stops)
