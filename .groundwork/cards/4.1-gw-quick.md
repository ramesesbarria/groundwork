---
id: 4.1
title: Light path: gw-quick
phase: 4
status: done
depends_on: [3.2]
---
## Goal
A light path for small changes, so process cost matches the size of the change (L-013).

## Acceptance criteria
- [x] `core/commands/gw-quick.md`: one session, no separate roles, no card file; tests must pass before a commit
- [x] It says when to use it (small, low-risk, one concern) and when to stop and make a card instead
- [x] It still follows LESSONS and the approval mode, and leaves a one-line entry in HANDOFF
- [x] `workflow.md` and the AGENTS.md template mention it in one line each

## Evidence
- Tests before: 14 fail ([output](../evidence/4.1/failing-before.txt)); after: 167 pass ([output](../evidence/4.1/test-output.txt)); build OK ([output](../evidence/4.1/build-output.txt))
- Reviewer: all criteria met. Checked against L-003 (it *is* the lean path), L-006, L-013. The snapshot change was reviewed: only the new skill and its entry in CLAUDE.md.
  - `gw-quick` points to `gw-ui-spec`, which arrives in card 4.5.

## History
- 2026-09-24 tests → implementation → review; committed
- 2026-09-24 created by planner after the dogfood run
