---
id: 0.2
title: Core templates
phase: 0
status: done
depends_on: [0.1]
---
## Goal
The template files that `init` copies into a user's project.

## Acceptance criteria
- [x] `core/templates/` has: AGENTS.md, SPEC.md, HANDOFF.md, LESSONS.md, card.md, decision.md, config.json
- [x] The AGENTS.md template is under 2,000 tokens (chars ÷ 4) and points into `.groundwork/` instead of repeating it
- [x] `config.json` has a JSON Schema covering approval mode, token budget, and test/lint/build commands
- [x] A test checks that every template has its required sections and the card template's frontmatter matches SPEC §8

## Evidence
- Tests: `vitest run` → 16 passed, 13 of them new ([output](../evidence/0.2/test-output.txt))
- Build: `tsc` succeeds ([output](../evidence/0.2/build-output.txt))
- Token budget: AGENTS.md template ≈ 364 tokens, well under 2,000 ([estimate](../evidence/0.2/token-estimate.txt))
- Reviewer: all criteria met. Checked against L-003 (lean: no new dependencies) and L-006 (honesty):
  - The config test checks required keys, allowed keys and the approval-mode enum. It is **not** full JSON Schema validation; that can come with `doctor` if it's needed.
  - A test ties the card schema to the example in SPEC §8, so the spec and the code can't drift apart silently.
  - Added beyond the card: `config.schema.json` (needed for the schema criterion), `.gitattributes` (LF line endings), and SPEC.md moved to `.groundwork/` as noted in HANDOFF.
  - Fixed during review: the HANDOFF template had a `{{phase}}` placeholder that has no value before planning; it now says "not planned yet".

## History
- 2026-09-24 tester → implementer → reviewer done; awaiting approval
- 2026-09-24 approved by human
