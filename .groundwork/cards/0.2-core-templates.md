---
id: 0.2
title: Core templates
phase: 0
status: todo
depends_on: [0.1]
---
## Goal
The template files that `init` copies into a user's project.

## Acceptance criteria
- [ ] `core/templates/` has: AGENTS.md, SPEC.md, HANDOFF.md, LESSONS.md, card.md, decision.md, config.json
- [ ] The AGENTS.md template is under 2,000 tokens (chars ÷ 4) and points into `.groundwork/` instead of repeating it
- [ ] `config.json` has a JSON Schema covering approval mode, token budget, and test/lint/build commands
- [ ] A test checks that every template has its required sections and the card template's frontmatter matches SPEC §8

## Evidence
<!-- linked files in .groundwork/evidence/0.2/ ; /gw-approve refuses if empty -->

## History
