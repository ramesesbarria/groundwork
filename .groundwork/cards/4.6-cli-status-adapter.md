---
id: 4.6
title: groundwork status and adapter
phase: 4
status: done
depends_on: [4.3]
---
## Goal
The two small CLI commands that round out v0.2.

## Acceptance criteria
- [x] `groundwork status`: phase, current card, cards by status, and blocked cards with what blocks them, read from files only
- [x] `groundwork adapter add <claude-code|opencode>`: adds or refreshes an adapter in an installed project, asking before overwriting changed files
- [x] Both have tests against a temp project

## Evidence
- Tests before: fail, modules missing ([output](../evidence/4.6/failing-before.txt)); after: 242 pass ([output](../evidence/4.6/test-output.txt)); build OK ([output](../evidence/4.6/build-output.txt))
- **Real use:** `groundwork status` on this repo's own cards found that card 2.2 still said `testing` after approval. Corrected, and logged as L-016 ([output, before and after](../evidence/4.6/status-on-this-repo.txt))
- Reviewer: all criteria met. Checked against L-003, L-006, L-015, L-016.
  - `adapter add` builds from the project's own `.groundwork/`, so custom commands get adapter files too (tested).
  - `init` and `adapter add` share one file-writing module (`files.ts`), with the same ask-before-overwrite, append and merge rules.
  - During this card, a separate fix commit removed 38 files that card 4.3 had installed into this repo by accident (L-015), and `init` now refuses to run in Groundwork's own source repo.

## History
- 2026-09-24 tests → implementation → real run on this repo (found L-016) → review; committed
- 2026-09-24 created by planner after the dogfood run
