---
id: 4.3
title: OpenCode adapter
phase: 4
status: todo
depends_on: [4.2]
---
## Goal
First-class OpenCode support, so Groundwork works with any model OpenCode runs (e.g. DeepSeek).

## Acceptance criteria
- [ ] File formats checked against current OpenCode docs, with the source noted on the card
- [ ] A pure generator: commands → OpenCode commands, roles → OpenCode agents (the reviewer can't edit files), guards → an OpenCode plugin where supported
- [ ] `init --adapter opencode` works, with snapshot and idempotence tests like the Claude Code adapter
- [ ] Anything OpenCode can't do is stated, not faked

## Evidence

## History
- 2026-09-24 created by planner after the dogfood run
