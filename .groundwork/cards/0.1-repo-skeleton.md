---
id: 0.1
title: Repo skeleton
phase: 0
status: done
depends_on: []
---
## Goal
A working repo shell so later cards have somewhere to land: README stub, folder layout from SPEC §12, and a TypeScript CLI package with tests and CI.

## Acceptance criteria
- [x] Folders exist: `core/`, `adapters/claude-code/`, `guards/`, `cli/`, `examples/`, `docs/`
- [x] `cli/` builds with TypeScript and runs `vitest` with one passing smoke test
- [ ] CI (GitHub Actions) runs build and tests on Windows and Ubuntu
- [x] README stub has the name, the one-line tagline and "work in progress"
- [x] package name is `groundwork-ai` (npm name decision, SPEC §15.4)

## Evidence
- Tests: `vitest run` → 3 passed ([output](../evidence/0.1/test-output.txt))
- Build + run: `tsc` succeeds, `groundwork --version` and usage work ([output](../evidence/0.1/build-and-run.txt))
- CI: `.github/workflows/ci.yml` written (Ubuntu + Windows × Node 22/24). **Not verified yet**: it needs a push to GitHub. Criterion left unchecked until the first run passes.
- Reviewer: all other criteria met. Checked against L-003 (kept lean: 1 runtime file, 3 dev deps, no linter yet). Note: the test file isn't type-checked (vitest strips types). Acceptable for now; revisit if tests grow.

## History
- 2026-09-24 tester → implementer → reviewer done; awaiting approval
- 2026-09-24 approved by human; CI to be confirmed on first push
