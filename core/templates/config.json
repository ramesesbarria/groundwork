{
  "$schema": "./config.schema.json",
  "approvalMode": "per-card",
  "experience": "new",
  "tokenBudget": 2000,
  "guards": ["no-ai-trailers"],
  "commitFormat": "[{id}] {title}",
  "commands": {
    "install": "",
    "test": "",
    "lint": "",
    "build": "",
    "run": ""
  }
}
