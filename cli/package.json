{
  "name": "groundwork-ai",
  "version": "0.0.0",
  "description": "A tool-agnostic workflow for building software with AI agents.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "groundwork": "dist/bin.js"
  },
  "files": [
    "dist",
    "core"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && vitest run",
    "prepack": "node scripts/copy-core.mjs",
    "postpack": "node scripts/copy-core.mjs --clean"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
